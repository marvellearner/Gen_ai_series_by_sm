# OpenAI Configuration
# Note: You only need OpenAI for the generation step (LLM). 
# Embeddings (HuggingFace), Retrieval database (Chroma), and Reranker (FlashRank) all run locally!
OPENAI_API_KEY="your_openai_api_key_here"
